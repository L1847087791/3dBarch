## 任务说明

1. 完成3D柱状图的交互事件，比如鼠标悬停，鼠标点击等

## 终极目标

1. 最终业务场景可能需要点击外部柱状图，摄像机移动到该柱状图近景（可完整看到内部每个分层），鼠标移动悬停到每个分层时，该层闪烁，如果点击内层，则会跳出tootiltip展示一些信息。

## 任务步骤

1. 由于涉及到内层和外层的拾取事件，普通的射线追踪法可能无法满足（内层永远被外层遮挡），但是如果使用GPU拾取则会大大增加开发难度，在这里需要取舍，比如在特定时机来设置外层无法拾取。如果射线追踪法能够满足以上需求，则进行如下步骤：
2. 先利用射线追踪法完成对每个柱状图的点击交互（控制台打印该柱状图对象），这里先不考虑摄像机跳转，这个部分可能有些麻烦。
3. 在适当时机设置外层不可拾取，从而可以与内层交互，完成悬停闪烁，点击（控制台打印该层对象）

## 注意事项

1. 先分析可不可以使用射线追踪法，给出限定条件，我目前的想法是点击柱状图后摄像机移动，此时这个柱状图外层不可拾取，这样即可拾取该柱状图内层。如果点击其他柱状图，则恢复当前柱状图的外层可拾取，以此内推。
2. 当前任务有两大步骤，步骤一是完成柱状图的拾取，步骤二是内层拾取。请依次完成，完成步骤一后我需要先测试，测试完毕再完成步骤二
3. 写代码时要和当前代码风格保持一致，确保代码可维护、可拓展、避免复杂化



