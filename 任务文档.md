## 任务说明

1. 每个柱状图内部不再是单纯的一个内层，而是会有多个分层，用于区分柱状图内部的不同数据
2. 注释掉worker传输部分(暂停模拟实时传输部分)，你先直接初始化一个默认的初始分层，柱状堆A每个柱状图20个分层、B每个30分层、C每个10个分层.

## 注意事项

1. 柱状图内部分层时，每层之间需要有一定间隔（可以很小但是肉眼能看出来），柱状图内层高度应该自适应外层柱状图（即内层高度之和+ 间隔 =外层高度），外层柱状图后期不再是统一高度（这个地方先不用更改）

2. 柱状图内部每个分层也是建立一个单独的Mesh（后期需要添加3D交互，确保每层能够拾取）

3. 编写的代码需要可维护，保持和原代码一致的风格，尽量新增代码、如果有必要可以修改部分代码

4. 全程使用中文回答我，中途不需要生成文档，只需要在完成后在README.md中补充修改任务进度

   

