## 任务需求

1. 实现3D柱状图数据渲染，后端实时传输数据，柱状图实时更新。
2. node版本为目前终端版本，即v22.14.0，react版本为16系列
3. 关于3D柱状图渲染，这里使用three.js，可以结合React使用

## 细节注意

1. 柱状图数量在真实项目中不会超过100，目前你仅需渲染80个柱状图
2. 先利用three.js搭建一个基础框架，确定场景、摄像机（在这个场景下你需要考虑使用正交相机还是透视相机），并把80个柱状图物体搭建出来
3. 颜色注意：场景整体为淡灰色，柱状图外壳为透明白色，内部实体高度为白色（取决与后端真实数据）
4. 目前不需要你写后端真实数据，利用worker线程以生成器+sleep的形式模拟后端真实数据流，然后前端用轮询的方式获取数据，进行每个柱状图的实时渲染更新
5. 交互方面：最初相机视角需要看到完整的80个柱状图（距离较远），之后可以利用滚轮和鼠标进行视角的拉近和远离（平移功能暂时可不用添加）

## 最终要求

1. 如果还有不清晰的地方，你可以再向我询问细节，如果没有可以分析执行计划并由我同意开始执行
2. 全程不需要生成额外的说明文档，只需要在开始时分析一次文档说明，在最后完成时再生成一次功能说明即可
3. 实现步骤需要清晰，先从实现功能开始，之后进行样式调整、最后进行性能优化
4. 项目结构需要清晰，尽量可复用

